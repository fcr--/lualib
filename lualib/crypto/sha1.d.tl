-- if binary is true, then the resulting digest will be returned as a 20 byte (160bit) string.
-- otherwise (by default) the digest will be returned as a 40-hex-chars-long string.
local type sha1 = function(message: string, binary: boolean): string

return sha1
